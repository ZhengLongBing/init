## 📝 文档版本控制说明

### 场景 A：完整跟踪（当前推荐）
- **generated/**：AI 生成的草稿，选择性提交
- **final/**：最终确定的文档，纳入版本控制
- **templates/**：模板文件，必须提交

### 场景 B：仅跟踪最终版本
1. 在 `generated/` 目录工作，不提交草稿
2. 确认无误后移至 `final/` 目录并提交
3. 使用 Git 标签标记重要版本（如 v1.0.0）

**示例工作流**：
```bash
# 1. 在 generated/ 目录创建草稿
echo "草稿内容" > docs/generated/新规范.md

# 2. 审查并移至 final/
mv docs/generated/新规范.md docs/final/新规范.md

# 3. 提交
git add docs/final/新规范.md
git commit -m "docs: 添加新规范文档"

# 4. 打标签（可选）
git tag -a v1.1.0 -m "添加新规范"
```
